/* eslint-env webextensions */

/* Disable links while holding ⌘+⌥ */
/* === */
// window.addEventListener("keydown", function (key) {
//   if (key.code === "AltLeft" || key.code === "MetaLeft") {
//     if (key.altKey && key.metaKey) chrome.runtime.sendMessage({event: "keyDown"})
//   }
// }, false)
// window.addEventListener("keyup", function (key) {
//   if (key.code === "AltLeft" || key.code === "MetaLeft") {
//     chrome.runtime.sendMessage({ event: "keyUp" })
//   }
// }, false)

/* ======= */
/* <HIDER> */
/* ======= */

// TODO
// Twitter
// ProductHunt
// palette
// crypto

/* -------- */
/* </HIDER> */
/* --------- */
